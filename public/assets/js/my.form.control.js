(function($) {
    
    //Default Settings OnLoad
    $(window).load(function() {
    
    
      //uid_site = "bussiness1";
      
      //Populate Form.Init
      if (uid_site !== "") {
        pop_forminit(uid_site);
      }
    
    });

//End of Script
})(jQuery);
